<div id="primary" class="p-t-b-30 height-full">
    <div class="container">

            
        <!-- Sign in form -->
        <div class="row">
            <div class="col-md-10 mx-md-auto">
                <div class="mt-5">
                    <div class="row grid">
                        <div class="col-md-5  p-5">
                            <h1 class="mt-3 mb-6 font-weight-lighter">Modifier mon profil</h1>
                            <br>
                                <div class="mt-3">
                                    <%= link_to "Modifier mon compte", edit_user_registration_path(current_user), class:"btn btn-outline-primary mr-4" %>
                                    <%= link_to 'Retour vers mon profil', @user, class:"btn btn-outline-secondary mr-4 mt-4" %>
                                </div>
                        </div>
                        <div class="col card p-5" style="width:450px;">
                            <div class="row mb-4 p-3">
                                <h5 class="mb-3">Ajouter une photo de profil</h5>
                                <%= form_tag user_avatars_path(@user), multipart: true do %>
                                    <%= file_field_tag :avatar %>
                                    <%= submit_tag "Ajoutez votre photo", class:"btn btn-outline-primary btn-sm" %>
                                <% end %>
                            </div>
                                <!-- Input -->
                                <div class="body">
                                    <!-- Input user name-->
                                    <%= form_with(model: user, local: true, method: :put) do |update| %>

                                    <!-- Input user description-->
                                    <div class="form-group form-float  mr-5">
                                        <div>
                                            <h5 class="mb-1 mt-2">Description</h5>
                                        </div>
                                        <div class="form-group">
                                            <%= update.text_field :description, class:"form-control" %>
                                        </div>
                                        <br>
                                    </div>
                                    <input type="submit" class="btn btn-outline-primary btn-sm pl-4 pr-4" value="Valider">
                                    <% end %>
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</div>

